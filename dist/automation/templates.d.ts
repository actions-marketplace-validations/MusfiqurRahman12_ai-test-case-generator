/**
 * Playwright Code Templates — common patterns for automated test generation.
 *
 * These templates are used as fallbacks and structure guides when the AI
 * generates Playwright test code.
 */
export declare const PLAYWRIGHT_CONFIG_TEMPLATE = "import { defineConfig, devices } from '@playwright/test';\n\n/**\n * Playwright configuration \u2014 auto-generated by AI Test Case Generator.\n * See https://playwright.dev/docs/test-configuration\n */\nexport default defineConfig({\n  testDir: './tests/e2e',\n  fullyParallel: true,\n  forbidOnly: !!process.env.CI,\n  retries: process.env.CI ? 2 : 0,\n  workers: process.env.CI ? 1 : undefined,\n  reporter: 'html',\n\n  use: {\n    // TODO: Update baseURL to your application's URL\n    baseURL: process.env.BASE_URL || 'http://localhost:3000',\n    trace: 'on-first-retry',\n    screenshot: 'only-on-failure',\n  },\n\n  projects: [\n    {\n      name: 'chromium',\n      use: { ...devices['Desktop Chrome'] },\n    },\n  ],\n\n  // TODO: Configure your dev server\n  // webServer: {\n  //   command: 'npm run dev',\n  //   url: 'http://localhost:3000',\n  //   reuseExistingServer: !process.env.CI,\n  // },\n});\n";
export declare function generateTestFileHeader(featureArea: string): string;
export declare function generateTestSkeleton(testCaseId: string, title: string, description: string, steps: Array<{
    stepNumber: number;
    action: string;
    expectedResult: string;
    testData?: string;
}>, preconditions: string[]): string;
export declare function generateAPITestSkeleton(testCaseId: string, title: string, description: string, steps: Array<{
    stepNumber: number;
    action: string;
    expectedResult: string;
    testData?: string;
}>): string;
//# sourceMappingURL=templates.d.ts.map